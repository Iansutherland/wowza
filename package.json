{
  "name": "myproject",
  "version": "0.0.0",
  "base": "/wowza/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "preview": "vite preview",
    "deploy": "vite build --base=/wowza/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "vite": "^2.3.3"
  },
  "dependencies": {
    "three": "^0.128.0"
  }
}
